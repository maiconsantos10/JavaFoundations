# 🧬 Herança e Polimorfismo em Java

Esta aula aborda dois dos pilares mais importantes da **Programação Orientada a Objetos (POO)** em Java:
- **Herança**
- **Polimorfismo**

Esses conceitos são amplamente utilizados em **back-end**, **Spring Boot**, **APIs REST** e **modelagem de domínio**.

---

## 1️⃣ Herança

### ✅ O que é Herança?
Herança permite que uma classe **herde atributos e métodos** de outra classe.

📌 Relação conceitual:
> **“É UM” (is-a)**

Exemplos:
- `Gerente` **é um** `Funcionario`
- `Analista` **é um** `Funcionario`
- `Cachorro` **é um** `Animal`

---

### 🔹 Palavra-chave: `extends`

```java
class ClasseFilha extends ClassePai {
}
A classe filha:

reutiliza código da classe pai

pode adicionar novos comportamentos

pode modificar comportamentos existentes

2️⃣ Exemplo Prático de Herança
🔹 Classe Pai: Funcionario
java
Copiar código
public class Funcionario {

    protected String nome;
    protected double salario;

    public Funcionario(String nome, double salario) {
        this.nome = nome;
        this.salario = salario;
    }

    public double calcularBonus() {
        return salario * 0.10; // bônus padrão de 10%
    }

    public void mostrarResumo() {
        System.out.println("Nome: " + nome);
        System.out.println("Salário: R$ " + salario);
        System.out.println("Bônus: R$ " + calcularBonus());
    }
}
🔍 Explicação detalhada
protected: permite acesso pelas subclasses

calcularBonus(): comportamento que pode ser sobrescrito

mostrarResumo(): usa o método calcularBonus()

🔹 Classe Filha: Gerente
java
Copiar código
public class Gerente extends Funcionario {

    private double adicional;

    public Gerente(String nome, double salario, double adicional) {
        super(nome, salario); // chama o construtor da classe pai
        this.adicional = adicional;
    }

    @Override
    public double calcularBonus() {
        return salario * 0.20 + adicional; // regra específica
    }
}
📌 Aqui ocorre:

reutilização de código

especialização do comportamento

sobrescrita com @Override

🔹 Classe Filha: Analista
java
Copiar código
public class Analista extends Funcionario {

    public Analista(String nome, double salario) {
        super(nome, salario);
    }

    @Override
    public double calcularBonus() {
        return salario * 0.15;
    }
}
