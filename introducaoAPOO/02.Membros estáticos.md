# 📚 Membros Estáticos em Java (`static`)

## 🎯 O que são Membros Estáticos?

Membros estáticos são **atributos ou métodos que pertencem à CLASSE**, e não aos objetos criados a partir dela.

Eles são declarados com a palavra-chave `static`.

📌 Em outras palavras:  
> Um membro estático existe **uma única vez** na aplicação e é compartilhado por todos os objetos da classe.

---

## 🧠 Por que usar `static`?

O uso de membros estáticos é indicado quando:

- ✅ O valor ou comportamento **não depende do objeto**
- ✅ A informação deve ser **compartilhada**
- ✅ Não faz sentido criar várias cópias do mesmo dado
- ✅ O método representa uma ação utilitária

Exemplos comuns:
- Constantes
- Contadores globais
- Métodos matemáticos
- Funções utilitárias

---

## ❌ Exemplo SEM `static`

```java
public class Contador {

    int total;
}
Cada objeto terá seu próprio total.

✅ Exemplo COM static
public class Contador {

    static int total;
}
Agora:

Existe apenas um total

Todos os objetos compartilham esse valor

▶️ Exemplo prático com contador
public class Produto {

    public static int totalProdutos;

    public Produto() {
        totalProdutos++;
    }
}
Uso:

Produto p1 = new Produto();
Produto p2 = new Produto();

System.out.println(Produto.totalProdutos);
📌 O valor pertence à classe, não aos objetos.

🧠 Acesso a membros estáticos
Forma correta:
Produto.totalProdutos;
Forma que funciona, mas NÃO é recomendada:
p1.totalProdutos;
👉 Acesso via classe deixa claro que o membro é estático.

🔧 Métodos Estáticos
Métodos estáticos também pertencem à classe.

public class Calculadora {

    public static int somar(int a, int b) {
        return a + b;
    }
}
Uso:

int resultado = Calculadora.somar(2, 3);
📌 Não é necessário criar objeto.

⚠️ Regra fundamental dos métodos estáticos
🚨 Métodos estáticos:

❌ NÃO acessam atributos de instância

❌ NÃO usam this

Isso porque não existe objeto associado.

❌ Exemplo inválido
public class Exemplo {

    int valor;

    public static void imprimir() {
        System.out.println(valor); // ERRO
    }
}
✅ Forma correta
public class Exemplo {

    static int valor;

    public static void imprimir() {
        System.out.println(valor);
    }
}
🧠 static + Encapsulamento
Membros estáticos também podem ser encapsulados.

public class Configuracao {

    private static int limite;

    public static int getLimite() {
        return limite;
    }

    public static void setLimite(int limite) {
        if (limite > 0) {
            Configuracao.limite = limite;
        }
    }
}
Uso:

Configuracao.setLimite(10);
System.out.println(Configuracao.getLimite());
🔐 Constantes com static final
A forma correta de criar constantes em Java:

public class Sistema {

    public static final double PI = 3.14159;
}
📌 Boas práticas:

static → pertence à classe

final → não pode ser alterado

Nome em CAIXA ALTA

⚠️ Erros comuns de iniciantes
❌ Usar static sem necessidade

❌ Misturar lógica de objeto com lógica estática

❌ Usar this em método estático

❌ Acessar membro estático via objeto

