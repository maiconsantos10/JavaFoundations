# 📚 Sobrecarga de Métodos em Java (Method Overloading)

## 🎯 O que é Sobrecarga?

Sobrecarga (Overloading) ocorre quando criamos **mais de um método com o MESMO nome**, porém com **assinaturas diferentes**.

📌 A assinatura de um método é definida por:
- Quantidade de parâmetros  
- Tipo dos parâmetros  
- Ordem dos parâmetros  

💡 **Definição resumida:**  
Sobrecarga permite que um mesmo método execute comportamentos diferentes, dependendo dos parâmetros que recebe.

---

## 🧠 Por que usar Sobrecarga?

A sobrecarga melhora muito a qualidade do código, pois:

- ✅ Deixa o código mais legível  
- ✅ Evita nomes de métodos desnecessários  
- ✅ Facilita manutenção  
- ✅ Aproxima o código da lógica do mundo real  

Em vez de criar vários métodos com nomes diferentes, usamos **um único nome**, e o Java decide qual versão executar.

---

## 🔧 Exemplo básico de Sobrecarga

```java
public class Calculadora {

    public int somar(int a, int b) {
        return a + b;
    }

    public double somar(double a, double b) {
        return a + b;
    }

    public int somar(int a, int b, int c) {
        return a + b + c;
    }
}
✔ Mesmo nome
✔ Parâmetros diferentes
✔ Sobrecarga aplicada corretamente

▶️ Utilização no main
public class Program {
    public static void main(String[] args) {

        Calculadora calc = new Calculadora();

        System.out.println(calc.somar(2, 3));        // int, int
        System.out.println(calc.somar(2.5, 3.5));    // double, double
        System.out.println(calc.somar(1, 2, 3));     // int, int, int
    }
}
📌 O Java identifica automaticamente qual método deve ser chamado em tempo de compilação.

🧠 Sobrecarga e Polimorfismo
A sobrecarga é considerada um tipo de:

Polimorfismo em tempo de compilação

Isso significa que o Java resolve qual método será executado antes do programa rodar, analisando apenas os tipos dos argumentos.

❌ O que NÃO é Sobrecarga
❌ Alterar apenas o tipo de retorno
public int calcular(int a, int b) { }
public double calcular(int a, int b) { }
🚨 Erro de compilação
O Java não diferencia métodos apenas pelo tipo de retorno.

❌ Criar métodos com nomes diferentes
somarInt()
somarDouble()
Funciona, mas quebra o conceito de sobrecarga e deixa o código menos elegante.

🔄 Sobrecarga com Métodos Estáticos
Sobrecarga funciona tanto com métodos de instância quanto com métodos static.

public class Util {

    public static int multiplicar(int a, int b) {
        return a * b;
    }

    public static double multiplicar(double a, double b) {
        return a * b;
    }
}
Uso:

System.out.println(Util.multiplicar(2, 3));
System.out.println(Util.multiplicar(2.5, 3.0));
🧱 Sobrecarga de Construtores
Sobrecarga é muito comum em construtores.

public class Produto {

    private String nome;
    private double preco;
    private int quantidade;

    public Produto(String nome, double preco) {
        this.nome = nome;
        this.preco = preco;
        this.quantidade = 0;
    }

    public Produto(String nome, double preco, int quantidade) {
        this.nome = nome;
        this.preco = preco;
        this.quantidade = quantidade;
    }
}
Uso:

Produto p1 = new Produto("TV", 1500);
Produto p2 = new Produto("Notebook", 3500, 5);
📌 Mesmo nome de construtor com parâmetros diferentes caracteriza sobrecarga.

🧠 Regras da Sobrecarga
✅ Pode variar:

Quantidade de parâmetros

Tipo dos parâmetros

Ordem dos parâmetros

❌ Não pode variar:

Apenas o tipo de retorno

⚠️ Erros comuns de iniciantes
❌ Confundir sobrecarga com sobrescrita

❌ Achar que comentários influenciam na escolha do método

❌ Chamar métodos com os mesmos tipos achando que muda a execução

Exemplo incorreto:

converter(2);
converter(2); // chama exatamente o mesmo método
