# 📚 Matrizes em Java (Arrays Bidimensionais)

## 🎯 O que são Matrizes?

Matrizes são **arrays com duas dimensões**.

Enquanto um vetor organiza dados em linha única,  
uma matriz organiza dados em **linhas e colunas**.

💡 Em termos simples:
> Uma matriz é um array de arrays.

---

## 🧠 Estrutura de uma Matriz

Uma matriz é declarada assim:

```java
int[][] matriz;

Ou já criando:

int[][] matriz = new int[3][4];

📌 Isso cria:

3 linhas

4 colunas

Índices começam em 0

🧱 Representação visual

Para:

int[][] matriz = new int[2][3];

Temos:

[ 0 ][ 0 ][ 0 ]
[ 0 ][ 0 ][ 0 ]

📌 matriz[linha][coluna]

🔧 Inicialização direta
int[][] matriz = {
    {1, 2, 3},
    {4, 5, 6}
};

📌 Aqui temos:

2 linhas

3 colunas

🧠 Acessando elementos
System.out.println(matriz[0][1]); // 2
System.out.println(matriz[1][2]); // 6
🔄 Alterando valores
matriz[1][0] = 99;
🧠 Percorrendo Matrizes
🔹 Usando for tradicional
for (int i = 0; i < matriz.length; i++) {
    for (int j = 0; j < matriz[i].length; j++) {
        System.out.println(matriz[i][j]);
    }
}

📌 matriz.length → número de linhas
📌 matriz[i].length → número de colunas

🔹 Usando foreach
for (int[] linha : matriz) {
    for (int valor : linha) {
        System.out.println(valor);
    }
}

📌 Mais simples quando não precisamos dos índices.

🧠 Matrizes Irregulares (Jagged Arrays)

Em Java, as matrizes podem ter linhas com tamanhos diferentes.

int[][] matriz = new int[3][];

matriz[0] = new int[2];
matriz[1] = new int[4];
matriz[2] = new int[1];

📌 Cada linha pode ter tamanho diferente.

🧠 Matrizes são Tipos Referência

Assim como vetores, matrizes são tipos referência.

int[][] a = {{1, 2}, {3, 4}};
int[][] b = a;

b[0][0] = 99;

System.out.println(a[0][0]); // 99

📌 Ambos apontam para a mesma matriz.

⚠️ Valores padrão

Quando criadas com new, as matrizes recebem valores padrão:

Tipo	Valor padrão
int	0
double	0.0
boolean	false
String	null
❌ Erros comuns de iniciantes

❌ Confundir linhas com colunas

❌ Esquecer que índices começam em 0

❌ Usar tamanho fixo errado

❌ Acessar posição inexistente

❌ Esquecer que matriz é tipo referência
