# 📚 Vetores (Arrays) em Java

## 🎯 O que são Vetores?

Vetores (arrays) são estruturas de dados usadas para **armazenar vários valores do mesmo tipo** em uma única variável.

📌 Cada valor ocupa uma posição (índice) dentro do vetor.

💡 Em termos simples:  
> Um vetor é uma coleção ordenada de elementos do mesmo tipo.

---

## 🧠 Características dos Vetores

- Tamanho **fixo** (definido na criação)
- Índices começam em **0**
- Acessados por índice
- Podem armazenar tipos primitivos ou referências
- São **tipos referência** em Java

---

## 🔧 Declaração e Criação de Vetores

### Forma 1 — Declarar e criar depois

```java
int[] numeros;
numeros = new int[5];
Forma 2 — Declarar e criar juntos
int[] numeros = new int[5];
📌 Esse vetor possui 5 posições: índices de 0 a 4.

🧱 Inicialização direta
int[] numeros = {10, 20, 30, 40, 50};
📌 O tamanho do vetor é definido automaticamente.

🧠 Acessando elementos do vetor
System.out.println(numeros[0]); // 10
System.out.println(numeros[3]); // 40
🔄 Alterando valores
numeros[1] = 99;
📌 Altera apenas a posição indicada.

⚠️ Índice inválido
numeros[5] = 10;
🚨 ArrayIndexOutOfBoundsException
➡️ Índice fora do tamanho do vetor.

🧠 Vetores e Laços de Repetição
for tradicional
for (int i = 0; i < numeros.length; i++) {
    System.out.println(numeros[i]);
}
📌 length indica o tamanho do vetor.

for-each (forma simplificada)
for (int n : numeros) {
    System.out.println(n);
}
📌 Ideal quando não precisa do índice.

🧠 Vetores como Tipo Referência
Vetores são tipos referência.

int[] a = {1, 2, 3};
int[] b = a;

b[0] = 99;

System.out.println(a[0]); // 99
📌 Ambos apontam para o mesmo vetor na memória.

🧪 Vetores em Métodos
public static void alterar(int[] vetor) {
    vetor[0] = 100;
}
int[] numeros = {1, 2, 3};
alterar(numeros);

System.out.println(numeros[0]); // 100
📌 A referência do vetor é copiada, mas o vetor é o mesmo.

🧠 Vetores e Valores Padrão
Quando criados com new, os vetores são inicializados automaticamente:

Tipo	Valor padrão
int	0
double	0.0
boolean	false
String	null
🧠 Vetores de Objetos
Produto[] produtos = new Produto[3];

produtos[0] = new Produto("TV", 1500);
produtos[1] = new Produto("Notebook", 3500);
📌 As posições armazenam referências para objetos.

⚠️ Vetor de objetos não inicializa os objetos
Produto[] produtos = new Produto[3];

produtos[0].getPreco(); // ERRO
🚨 NullPointerException
➡️ Cada objeto precisa ser instanciado.

❌ Erros comuns de iniciantes
❌ Confundir índice com tamanho

❌ Esquecer que índices começam em 0

❌ Achar que vetor cresce automaticamente

❌ Não usar length

❌ Esquecer que vetor é tipo referência

