# 📚 Boxing, Unboxing e Wrapper Classes em Java

## 🎯 O que são Wrapper Classes?

**Wrapper Classes** são classes que representam os **tipos primitivos** como **objetos**.

📌 Cada tipo primitivo possui uma classe wrapper correspondente.

💡 Em termos simples:  
> Wrapper classes permitem tratar valores primitivos como objetos.

---

## 🧠 Tipos Primitivos x Wrapper Classes

| Tipo Primitivo | Wrapper |
|---------------|---------|
| `int` | `Integer` |
| `double` | `Double` |
| `char` | `Character` |
| `boolean` | `Boolean` |
| `byte` | `Byte` |
| `short` | `Short` |
| `long` | `Long` |
| `float` | `Float` |

📌 As wrappers pertencem ao pacote `java.lang`.

---

## 🧱 Por que Wrapper Classes existem?

Wrapper classes são necessárias porque:

- ✅ Coleções (`ArrayList`, `List`, `Map`) não aceitam tipos primitivos
- ✅ Permitem uso de métodos utilitários
- ✅ Podem representar ausência de valor (`null`)
- ✅ Facilitam integração com APIs

---

## 🧠 Boxing

**Boxing** é o processo de **converter um tipo primitivo em um objeto wrapper**.

### 🔧 Exemplo de Boxing manual

```java
int x = 10;
Integer obj = Integer.valueOf(x);
🔄 Autoboxing
O Java faz o boxing automaticamente.

int x = 10;
Integer obj = x;
📌 O compilador converte automaticamente.

🧠 Unboxing
Unboxing é o processo de converter um objeto wrapper em um tipo primitivo.

🔧 Exemplo de Unboxing manual
Integer obj = Integer.valueOf(20);
int x = obj.intValue();
🔄 Auto-unboxing
Integer obj = 20;
int x = obj;
📌 O Java converte automaticamente.

⚠️ Wrapper Classes e null
Wrapper classes podem ser null.

Integer x = null;
int y = x; // ERRO
🚨 NullPointerException
➡️ Auto-unboxing tenta extrair valor de null.

🧠 Wrapper Classes em Coleções
ArrayList<Integer> lista = new ArrayList<>();

lista.add(10); // autoboxing
lista.add(20);

int soma = lista.get(0) + lista.get(1); // auto-unboxing
📌 Por baixo dos panos, o Java converte automaticamente.

🧠 Comparação: == vs equals
❌ Usando ==
Integer a = 100;
Integer b = 100;

System.out.println(a == b); // true
⚠️ Cuidado
Integer a = 200;
Integer b = 200;

System.out.println(a == b); // false
📌 Isso ocorre por causa do Integer Cache (-128 a 127).

✅ Usando equals
System.out.println(a.equals(b)); // true
📌 Sempre use equals para comparar wrappers.

🧠 Wrapper Classes e Métodos Utilitários
int x = Integer.parseInt("123");
double y = Double.parseDouble("3.14");
📌 Conversão de String para tipo primitivo.

❌ Erros comuns de iniciantes
❌ Usar == para comparar wrappers

❌ Esquecer que wrappers podem ser null

❌ Não entender autoboxing/unboxing

❌ Achar que wrapper e primitivo são iguais

❌ Unboxing de null
