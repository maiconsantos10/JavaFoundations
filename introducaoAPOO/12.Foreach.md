# 📚 Laço foreach em Java

## 🎯 O que é o Laço foreach?

O `foreach` é uma forma simplificada de percorrer **vetores (arrays)** e **coleções** em Java.

Ele é conhecido como **laço aprimorado (enhanced for)**.

💡 Em termos simples:
> O `foreach` percorre todos os elementos de uma estrutura automaticamente, sem precisar de índice.

---

## 🧠 Quando usar o foreach?

Use o `foreach` quando:

- ✅ Você precisa apenas ler os elementos
- ✅ Não precisa do índice
- ✅ Não precisa modificar a estrutura
- ✅ Quer código mais limpo e legível

---

## 🧱 Sintaxe do foreach

```java
for (Tipo variavel : estrutura) {
    // código
}
📌 Onde:

Tipo → tipo dos elementos

variavel → variável temporária

estrutura → array ou coleção

🔧 Exemplo com Vetor (Array)
int[] numeros = {10, 20, 30, 40};

for (int n : numeros) {
    System.out.println(n);
}
📌 O laço percorre automaticamente todas as posições do vetor.

🔄 Comparação com o for tradicional
🔹 For tradicional
for (int i = 0; i < numeros.length; i++) {
    System.out.println(numeros[i]);
}
🔹 Foreach
for (int n : numeros) {
    System.out.println(n);
}
📌 O foreach é mais simples quando o índice não é necessário.

🧠 Exemplo com Array de Objetos
Produto[] produtos = new Produto[2];

produtos[0] = new Produto("TV", 1500);
produtos[1] = new Produto("Notebook", 3500);

for (Produto p : produtos) {
    System.out.println(p.getNome());
}
📌 A variável p representa cada elemento do array.

🧠 Exemplo com ArrayList
ArrayList<String> nomes = new ArrayList<>();

nomes.add("Maicon");
nomes.add("João");

for (String nome : nomes) {
    System.out.println(nome);
}
📌 Funciona perfeitamente com coleções.

⚠️ Importante: Não altera a estrutura original
for (int n : numeros) {
    n = 100;
}
📌 Isso NÃO altera o vetor original.
n é apenas uma cópia temporária.

❌ Erros comuns de iniciantes
❌ Achar que pode acessar índice dentro do foreach

❌ Tentar alterar a estrutura usando a variável do laço

❌ Usar foreach quando precisa do índice

❌ Confundir a variável temporária com o elemento real

🧠 Quando NÃO usar foreach?
Não use foreach quando:

Você precisa do índice

Precisa percorrer parcialmente

Precisa modificar posições específicas

Precisa iterar ao contrário

Nesses casos, use o for tradicional.

