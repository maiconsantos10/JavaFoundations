# 📚 Modificadores de Acesso em Java

## 🎯 O que são Modificadores de Acesso?

Modificadores de acesso definem **de onde** uma classe, atributo, método ou construtor pode ser acessado.

📌 Eles controlam a **visibilidade** dos membros de uma classe, sendo fundamentais para:
- Encapsulamento
- Segurança
- Organização do código

---

## 🧠 Por que Modificadores de Acesso são importantes?

Sem controle de acesso:
- ❌ Qualquer parte do sistema altera qualquer coisa
- ❌ Código inseguro
- ❌ Manutenção difícil

Com modificadores de acesso:
- ✅ Código protegido
- ✅ Regras bem definidas
- ✅ Menos efeitos colaterais
- ✅ Estrutura profissional

---

## 🔐 Tipos de Modificadores de Acesso

Java possui **4 níveis de acesso**:

| Modificador | Classe | Pacote | Subclasse | Mundo |
|------------|-------|--------|-----------|-------|
| `public` | ✔ | ✔ | ✔ | ✔ |
| `protected` | ✔ | ✔ | ✔ | ❌ |
| *(default)* | ✔ | ✔ | ❌ | ❌ |
| `private` | ✔ | ❌ | ❌ | ❌ |

---

## ✅ `public`

Permite acesso **de qualquer lugar**.

```java
public class Produto {
    public String nome;
}
📌 Uso comum:

Classes principais

APIs públicas

Métodos acessados externamente

⚠️ Evite atributos public.

🔒 private
Permite acesso apenas dentro da própria classe.

public class Produto {

    private double preco;
}
📌 Uso ideal para:

Atributos

Regras internas

Detalhes de implementação

👉 Base do encapsulamento.

🧱 protected
Permite acesso:

Dentro da classe

Dentro do mesmo pacote

Em subclasses (herança)

public class Funcionario {

    protected double salario;
}
📌 Muito usado em herança.

📦 Modificador default (sem palavra-chave)
Quando nenhum modificador é informado, o acesso é restrito ao mesmo pacote.

class Produto {
    String nome;
}
📌 Também chamado de package-private.

🧠 Modificadores de acesso em Métodos
public class Conta {

    private double saldo;

    public double getSaldo() {
        return saldo;
    }

    protected void ajustarSaldo(double valor) {
        saldo += valor;
    }
}
📌 Controle quem pode ler e quem pode modificar.

🧱 Modificadores de acesso em Construtores
public class Usuario {

    private String nome;

    public Usuario(String nome) {
        this.nome = nome;
    }

    private Usuario() {
    }
}
📌 Construtores private são usados em:

Singleton

Classes utilitárias

Controle de criação de objetos

🔧 Modificadores de acesso e static
public class Config {

    private static int limite;

    public static int getLimite() {
        return limite;
    }
}
📌 Encapsulamento funciona normalmente com membros estáticos.

⚠️ Erros comuns de iniciantes
❌ Usar tudo como public

❌ Não entender default

❌ Confundir protected com public

❌ Quebrar encapsulamento com atributos públicos

❌ Ignorar visibilidade em projetos grandes

